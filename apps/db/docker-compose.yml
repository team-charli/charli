version: '3.1'

services:
  db:
    build: .
    env_file:
      - ./.env
    ports:
      - "5432:5432"

  pgadmin:
    image: dpage/pgadmin4
    env_file:
      - ./.env
    ports:
      - "5050:80"
    depends_on:
      - db
